// WAP to print the length of a string use of str function.

#include<stdio.h>
#include<string.h>
int main()
{
    char ch[50]="dev";
    int length = strlen(ch);
    printf("%d", length);
    return 0;
}